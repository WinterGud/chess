#include "Cell.h"

Cell::Cell(int x, int y, int w, int h, Color color, const std::string& texturePath)
    : BaseEntity(x, y, w, h, color, texturePath)
{
}

void Cell::setCoord(int x, int y)
{
    
}

void Cell::setCoord(Coord coord)
{
    
}
